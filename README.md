# Anatoly Nikiforov — Professional Portfolio

## Quick Start

```bash
gem install bundler jekyll
bundle install
bundle exec jekyll serve
```

Open http://localhost:4000

## Deploy

```bash
git add .
git commit -m "Update"
git push origin main
```

Site: https://nikiforidi.github.io
